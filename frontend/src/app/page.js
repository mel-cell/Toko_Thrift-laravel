"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { login, logout, getCurrentUser, isAuthenticated } from "../lib/auth";
import main from "../app/(user)/page"

export default function Home() {
  return (

  );
}
